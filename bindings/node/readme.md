# `graphis-node`

This directory contains the NodeJS bindings for Graphis. To test the binding, install the dependencies using `npm i`, then run `npm run build` to produce the binaries. The bindings are produced by [napi.rs](https://napi.rs/). 
