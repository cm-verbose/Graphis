# Core

This is the core library for Graphis. This project runs on rust, and contains the code ran by the bindings. To ensure that the tests work, install cargo tarpaulin by using `cargo install cargo-tarpaulin`.
