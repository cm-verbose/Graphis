# Core

This is the core library for Graphis. This project runs on [Rust](https://rust-lang.org/) and contains the code run by the bindings. To ensure that the tests work, install [cargo tarpaulin](https://crates.io/crates/cargo-tarpaulin) by using `cargo install cargo-tarpaulin`.
